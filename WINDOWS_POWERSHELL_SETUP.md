# Windows PowerShell Setup

This guide has been merged into `README.md`.

Use `README.md` as the canonical setup and run guide:
- `## Windows Setup`
- `## One-Click Start (Windows)`
- `## Run (Same LAN, iPhone)`
